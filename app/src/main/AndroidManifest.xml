<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.duaduatib.eduforum">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_eduforum"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_eduforum_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EduForum"
        tools:targetApi="31"
        android:requestLegacyExternalStorage="true" >


        <activity
            android:name=".welcome_page"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login_page"></activity>
        <activity android:name=".choose_role_page"></activity>
        <activity android:name=".daftar_dosen_page"></activity>
        <activity android:name=".daftar_mahasiswa_page"></activity>
        <activity android:name=".beranda"></activity>
        <activity android:name=".profil_user"></activity>
        <activity android:name=".DetailPostActivity"></activity>
    </application>

</manifest>